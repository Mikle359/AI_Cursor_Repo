# AGENTS.md

## Cursor Cloud specific instructions

This is an empty sandbox repository ("Repo for testing AI with Cursor"). As of the initial setup there are no source files, dependencies, build systems, or services.

- **Tech stack:** None yet. The repo contains only `README.md`.
- **Lint / Test / Build / Run:** No commands available — add a project first.
- **Environment:** The VM has git, Node.js (v22 via nvm), Python 3.12, npm, and pnpm pre-installed.
- **Update script:** `echo "No dependencies to install"` — replace once a project is bootstrapped.

When code is added to this repo, update this file with relevant lint/test/build/run instructions and adjust the VM update script accordingly.
